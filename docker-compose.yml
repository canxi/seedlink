version: '3.8'

services:
  app:
    image: ghcr.io/canxi/seedlink:latest
    container_name: seedlink
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
      - /downloads:/downloads
      - /media:/media
    environment:
      - TZ=Asia/Shanghai
    restart: unless-stopped
